//==============================================================================//Name://  toggleDisplayLocalJointAxis.mel//==============================================================================//Purpose://if keysPressed == "" 					 	// - hide Joint Local Axis//if keysPressed == "Ctrl" 		 		 	// - display Joint Local Axis//==============================================================================//Usage://  1. Save the toggleDisplayLocalJointAxis.mel to your scripts directory.//  2. Start Maya. In the command line type toggleDisplayLocalJointAxis();//  3. Middlemouse drag to a shelf you like.//==============================================================================global proc toggleDisplayLocalJointAxis(){string $keys = keysPressed();if ($keys == ""){for($joints in `ls-type joint`)	{	setAttr ($joints + ".displayLocalAxis") 0;//	setAttr ($joints + ".radius") 2;	setAttr ($joints + ".displayHandle") 0;	}print "hide Joint Local Axis";}if ($keys == "Ctrl"){for($joints in `ls-type joint`)	{	setAttr ($joints + ".displayLocalAxis") 1;//	setAttr ($joints + ".radius") 2;	setAttr ($joints + ".displayHandle") 1;	}print "display Joint Local Axis";}}