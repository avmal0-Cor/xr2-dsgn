//==============================================================================//Name://  MultilisterBoxSwatch.mel//==============================================================================//Purpose://  Переключение вида превьюшек в мультилистере на боксы.//==============================================================================//Usage://  1. Save the MultilisterBoxSwatch.mel to your scripts directory.//  2. Start Maya. In the command line type MaterialRenamer();//  3. Middlemouse drag to a shelf you like.//==============================================================================global proc MultilisterBoxSwatch(){string $Materials[] = `ls -mat`;$NumMats = size($Materials);int $i = 2;	for ($i = 2; $i < ($NumMats); $i++)	{	string $Shaders[] = `listConnections -type "shadingEngine" $Materials[$i]`;	listerEditor -e -primitive $Shaders box "multiListerPanel1TopTab1";        }listerEditor -e -sortType "alphabetical" -reSort "multiListerPanel1BottomTab1";}